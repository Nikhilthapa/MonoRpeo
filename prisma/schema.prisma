// Unified Prisma Schema
// Multi-file schema organized by category
// Prisma automatically discovers all .prisma files in this directory and subdirectories

generator client {
  provider = "prisma-client"
  output   = "../libs/backend/database/src/clients/generated"
}

datasource db {
  provider = "postgresql"
}
